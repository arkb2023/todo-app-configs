apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: todo-ingressroute
spec:
  entryPoints:
  - web
  routes:
  # Frontend route
  - match: PathPrefix(`/production`)
    kind: Rule
    services:
    - name: production-todo-app-svc
      port: 1234
    middlewares:
    - name: production-ingressroute-prefix-strip
  # Backend route  
  - match: PathPrefix(`/production/todos`)
    kind: Rule
    services:
    - name: production-todo-backend-svc
      port: 4567
    middlewares:
    - name: production-ingressroute-prefix-strip

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ingressroute-prefix-strip
spec:
  stripPrefix:
    prefixes:
    - /production
